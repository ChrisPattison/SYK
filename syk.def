Bootstrap: localimage
From: ./base.sif

%runscript
syk
#source /python/bin/activate
#${SCRIPT_PATH}/syk.py "$@"

%files
./src /src

%environment
export SCRIPT_PATH=/src
export PATH=${PATH}:${SCRIPT_PATH}/build
export PYTHON_PATH=${PYTHON_PATH}:${SCRIPT_PATH}/build

%labels

%post
cd /src


#rm -rf build
mkdir build
cd build

PREFIX=

${PREFIX} cmake3 .. -DCMAKE_BUILD_TYPE=Release -DEIGEN3_INCLUDE_DIR=/base/eigen3
${PREFIX} make VERBOSE=1 -j$(nproc)
${PREFIX} make install
